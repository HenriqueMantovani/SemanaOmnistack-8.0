import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

//Aqui estamos renderizando o arquivo APP.js ( Que tem as Rotas ) dentro da div root que esta no indes.html
ReactDOM.render(<App />, document.getElementById('root'));

